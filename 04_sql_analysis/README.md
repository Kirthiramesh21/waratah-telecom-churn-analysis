# Phase 4 — SQL Analysis

**Status:** Next up.

Planned: churn derivation under D1 (window functions over recharge gaps),
blended ARPU via UNION across the two revenue tables, segmentation by state /
plan / tenure / value decile, cohort retention curves, churn-driver analysis, and
campaign ROI.

Queries live in [`queries/`](./queries), one file per business question, each
opening with the question it answers and the requirement (BR/FR) it satisfies.
